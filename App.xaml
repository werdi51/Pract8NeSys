<Application x:Class="_7.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:_7"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <Style TargetType="Page">
            <Setter Property="Background"  Value="{DynamicResource PageBackground}"/>
        </Style>

        <Style TargetType="Grid">
            <Setter Property="Background" Value="{DynamicResource PageBackground}"/>
        </Style>

        <Style TargetType="Grid"
       x:Key="MainPanel">
            <Setter Property="Background" Value="{DynamicResource PageBackground}"/>
        </Style>

        <!---->

        <Style TargetType="StackPanel">
            <Setter Property="Margin" Value="10"></Setter>
        </Style>

        <!---->

        <Style TargetType="StackPanel" x:Key="HorisontalPanel">
            <Setter Property="Margin" Value="10"></Setter>
            <Setter Property="Orientation" Value="Horizontal"/>
        </Style>

        <!---->
        
        <Style TargetType="StackPanel" x:Key="PanelCenter">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="300"/>
        </Style>

        <!---->
        <Style TargetType="TextBlock" x:Key="header">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
        </Style>


        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
        </Style>

        <!---->
        <Style TargetType="Button">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Height="{TemplateBinding Height}"
                                Width="{TemplateBinding Width}"
                                CornerRadius="10">
                            <ContentPresenter Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="100"/>
            <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ButtonMouseOver}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ButtonPressed}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="ButtonAdd" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="ContentStringFormat" Value="+ {0}"> </Setter>
        </Style>

        <Style TargetType="Button" x:Key="ButtonChange" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="ContentStringFormat" Value="✎ {0}"></Setter>
        </Style>

        <Style TargetType="Button" x:Key="ButtonStart" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="ContentStringFormat" Value="▶ {0}"></Setter>
        </Style>

        <Style TargetType="Button" x:Key="ButtonExit" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="ContentStringFormat" Value="🚪 {0}"></Setter>
        </Style>

        <Style TargetType="Button" x:Key="ButtonDelete" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="ContentStringFormat" Value="❌ {0}"></Setter>
        </Style>
        <!---->
        <Style TargetType="Border" x:Key="BorderElement">
            <Setter Property="Background" Value="{DynamicResource BorderBackground}"/>
            <Setter Property="Padding" Value="10"/>
        </Style>
        <!---->
        <Style TargetType="ListView">
            <Setter Property="Height" Value="200"/>
            <Setter Property="Background" Value="{DynamicResource PageBackgroundList}"/>
        </Style>
        <!---->

        <Style TargetType="Menu">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Background" Value="{DynamicResource PageBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
        </Style>
        <!---->
        <!---->
        <!---->

    </Application.Resources>
</Application>
